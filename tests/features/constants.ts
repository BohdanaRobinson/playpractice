/** Shared config for feature tests — tune URLs/selectors counts for your app. */
export const LOGIN_URL = "https://example.com/login";
export const USERNAME = "Dana";
export const PASSWORD = "1234";

/** How many stocks to add (> 600 when you need that check). Lower while debugging selectors. */
export const STOCKS_TO_ADD = 605;
